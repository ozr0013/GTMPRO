# Hero image generation — see docs/LOCAL_IMAGES.md
#
#   pip install -r requirements-images.txt
#   npm run images:server
#
# Torch ships the right backend per platform automatically: CUDA wheels on
# Windows/Linux with an NVIDIA card, Metal (MPS) on Apple Silicon. No per-OS
# install file, no CUDA toolkit to match by hand.
#
# CPU-only machine? Install the smaller CPU wheel first, then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.2
diffusers>=0.27
transformers>=4.38
accelerate>=0.27
safetensors>=0.4
pillow>=10.0
